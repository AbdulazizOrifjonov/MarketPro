{
  "name": "marketpro",
  "version": "1.0.0",
  "private": true,
  "description": "MarketPro Uzbekistan - monolith (Express + React)",
  "scripts": {
    "install:all": "npm install --prefix apps/web && npm install --prefix apps/api",
    "build": "npm run build --prefix apps/web && npm run prisma:generate --prefix apps/api",
    "start": "npm run start --prefix apps/api",
    "dev:web": "npm run dev --prefix apps/web",
    "dev:api": "npm run dev --prefix apps/api"
  }
}
