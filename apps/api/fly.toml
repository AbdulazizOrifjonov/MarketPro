app = "marketpro-api"

[build]
  builder = "heroku/buildpacks:20"

[env]
  NODE_VERSION = "20"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
